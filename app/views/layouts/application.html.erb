<!DOCTYPE html>
<html>
  <head>
    <title>コンナビ！</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <%= favicon_link_tag() %>
  </head>

  <body>
    <header>
      <div class="header-logo">
        <%= link_to("ConNavi", "/") %>
      </div>
      <ul class="header-menus">
        <% if @current_user %>
        <li>
          <%= link_to("投稿する", "/posts/new") %>
        </li>
        <li>
          <%= link_to("コンビニ一覧", "/stores/index") %>
        </li>
        <li>
          <%= link_to("ユーザー一覧", "/users/index") %>
        </li>
        <li>
          <%= link_to(@current_user.name,"/users/#{@current_user.id}") %>
        </li>
        <% else %>
        <li>
          <% if @current_user %>
            <%= link_to("ConNavi", "/posts/index") %>
          <% else %>
            <%= link_to("ConNavi", "/") %>
          <% end %>
        </li>
        <li>
          <%= link_to("ConNaviとは", "/about") %>
        </li>
        <li>
          <%= link_to("新規登録", "/signup") %>
        </li>
        <li>
          <%= link_to("ログイン", "/login") %>
        </li>
        <% end %>
      </ul>
    </header>

      <footer>
        <ul class="footer">
          <li>
            <%= link_to("よくある質問", "/help") %>
          </li>
          <li class="footer">
            <%= link_to("利用規約", "/terms") %>
          </li>
          <li class="footer">
            <%= link_to("プライバシーポリシー", "/login") %>
          </li>
          <li class="footer">
            <%= link_to("運営会社", "/owner") %>
          </li>
        </ul>
      </footer>

    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
